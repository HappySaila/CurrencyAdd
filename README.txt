This program is to convert a string of currency values and output the answer.

The makefile will compile and run the code by using... make run
It will also test the code and build an html report... make test 
To create a report... make report
To all reports and compiled classes use... make clean
And finally to view report... google-chrome coveragereport/index.html